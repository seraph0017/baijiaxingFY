# MySQL Auth Admin Harness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Split public homepage and material-management backend into separate pages, add a user system, and make Harness configurable from the backend with MySQL persistence.

**Architecture:** Keep the existing Node.js no-framework runtime. Add a storage boundary in `server.js` backed by MySQL when `DATABASE_URL` or `MYSQL_*` is configured, with file fallback only for local development. Add `/login.html` and `/admin.html`; protect admin APIs with cookie sessions and roles.

**Tech Stack:** Node.js ESM, built-in `crypto/http/fs`, `mysql2`, HTML/CSS/JS, current verification scripts.

---

### Task 1: Verification First

**Files:**
- Modify: `verify-ui.mjs`
- Modify: `verify-server-auth.mjs`
- Modify: `verify-server-logic.mjs`
- Modify: `verify-site.mjs`

- [ ] Add failing checks that require `/admin.html` and `/login.html`, with `index.html` no longer containing the operations workspace.
- [ ] Add failing auth checks for `POST /api/auth/login`, `GET /api/auth/me`, `POST /api/auth/logout`, and role-protected admin APIs.
- [ ] Add failing Harness config checks for `GET/PUT /api/harness-config`, with API keys write-only.
- [ ] Add failing persistence checks for MySQL configuration support and documented schema/bootstrap behavior.

### Task 2: Server Storage And Auth

**Files:**
- Modify: `package.json`
- Modify: `server.js`
- Create generated by npm: `package-lock.json`

- [ ] Add `mysql2` dependency.
- [ ] Add storage helpers for workspace, feedback, audit, users, sessions, and Harness config.
- [ ] Add startup/health metadata showing whether MySQL is configured.
- [ ] Add password hashing and secure-ish cookie session handling for MVP.
- [ ] Add role checks: `admin` can configure Harness; `editor` can manage materials but cannot change Harness config.

### Task 3: Page Split And Frontend Wiring

**Files:**
- Modify: `index.html`
- Create: `admin.html`
- Create: `login.html`
- Modify: `assets/app.js`
- Modify: `assets/styles.css`

- [ ] Keep `index.html` focused on public search/detail/feedback.
- [ ] Move material editing, review queue, feedback desk, audit, workspace tools, and Harness configuration to `admin.html`.
- [ ] Add `login.html` for username/password sign-in.
- [ ] Make admin page call `/api/auth/me` before loading protected data.
- [ ] Make Harness generation use saved backend config.

### Task 4: Docs And Verification

**Files:**
- Modify: `README.md`
- Modify: `.env.example`
- Modify: `.gitignore`
- Modify: `Dockerfile`

- [ ] Document local MySQL creation, required env vars, and default admin bootstrap.
- [ ] Run `npm run verify:release`.
- [ ] Run `npm run verify:server` when localhost binding is available.
- [ ] Commit and push.
